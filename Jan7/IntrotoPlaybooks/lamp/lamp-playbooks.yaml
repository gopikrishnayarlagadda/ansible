---
 - name: install Lamp Stack
   hosts: all
   become: yes
   tasks:
    - name: install apache2 and update packages
      apt:
        name: apache2
        update_cache: yes
        state: present  
    - name: install php
      apt: 
        name: php
        state: present

